master-worker模式：是一种常用的并行设置模式，其核心的思想是，系统中有两个进程协议工作，master进程和worker进程。
    其中master进程是负责接收和分配任务，而worker进程是负责处理子任务。当各个子任务完成后，返回结果给master主任务，
    然后主任务进行汇总结果，返回给系统。
    此模式的一个好处就是将一个大的重的任务分解成多个子任务进行处理，可以大大加快处理速度和效率，并发执行。而对于系统请求者Client来说，
    任务一旦提交，Master进程就会立刻分配任务并立即返回，并不会等系统处理完全部任务再返回，其处理过程是异步的。